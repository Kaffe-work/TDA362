exercise 1

Save space, easier to define shapse

exercise 2

Fovy=Field of view

aspect ratio = width/height

near plane = Plane before wHich nothing will be rendered

far plane = Plane after wHIch nothing will be rendered


anisotropy:

retain the sharpness of a texture by averaging the minified texture

-> faraway pixels are less blurry